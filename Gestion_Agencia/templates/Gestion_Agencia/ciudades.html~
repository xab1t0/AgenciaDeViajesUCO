{% extends "base.html" %}
  {% block Titulo %}
    NUESTRAS CIUDADES
  {% endblock %}
  {% block Contenido %}
    {% for ciudades in listaCiudades %}
    <div class="col-sm-6 col-md-4">
      <div class="thumbnail">
        <img src="{{ciudades.imagen.url}}" width="260px" height="130px" style="margin-top:15px;"/>
        <div class="caption">
            <h3 style="text-align:center"><a href="{% url 'Gestion_Agencia:ciudad' ciudad_id=ciudades.id %}">{{ciudades.nombre}}</a></h3>
        </div>
      </div>
    </div>
    {% endfor %}
{% endblock %}
